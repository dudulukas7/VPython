Web VPython 3.2   

from vpython import *  

scene = canvas(title="Subtração de Vetores", width=600, height=400, background=color.black)  

x1 = float(input("Digite a componente X do primeiro vetor (A): "))   
y1 = float(input("Digite a componente X do segundo vetor (B): "))  
escalar = float (input("Digite o escalar: "))

vetor1 = vec(x1, y1, 0)  
vetor_novo = vetor1 * escalar 

seta1 = arrow(pos=vec(0, 0, 0), axis=vetor1, color=color.red, shaftwidth=0.1, opacity=0.7)   
seta_nova = arrow(vetor1 * escalar , axis=vetor_sub, color=color.orange, shaftwidth=0.1)  

label(pos=vec(2, 5, 0), text="Multiplicação por escalar", height=15, color=color.purple)  
